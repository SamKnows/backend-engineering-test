SamKnows Metric Analyser v1.0.0
===============================

Period checked:

    From: {{ data.dateStart|date("Y-m-d") }}
    To:   {{ data.dateEnd|date("Y-m-d") }}

Statistics:

    Unit: Megabits per second

    Average: {{ data.avg|Mbps }}
    Min: {{ data.min |Mbps }}
    Max: {{ data.max |Mbps }}
    Median: {{ data.median|Mbps }}
{% if data.underperformance is not empty %}

Investigate:

    * The period between {{ data.underperformance.start|date("Y-m-d") }} and {{ data.underperformance.end|date("Y-m-d") }}
      was under-performing.

{% endif %}